<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consulta</title>
    <link rel="stylesheet" href="/css/event.css">
</head>

<body>
    <div class="container">
        <div class="card">
            <div class="card header">
<hr>

    <h4>Consulta feita: <%= appo.description %> </h4> 
<hr>
        <div class="card-body">
        <h4>Paciente: <%= appo.name %> </h4>
        <h4>Email: <%= appo.email %> </h4>
        <h4>CPF:  <%= appo.cpf %> </h4>    
     
    <% if(appo.finished) { %>
        <form action="/finish" method="POST">
        <input type="hidden" name="id" value="<%= appo._id %>">
        <button class="btn btn btn-danger">Finalizar Consulta</button>
        </form>
    <% } %>
    </div>
    
            </div>
        </div>
    </div>
   
</body>
</html>